# EPUB/PDF Reader

Небольшая EPUB/PDF-читалка на React + Vite. Поддерживает открытие книги, навигацию по главам (EPUB) / страницам (PDF) и базовые настройки отображения текста.

## Возможности

### EPUB
- 📚 Чтение EPUB (через `epubjs` / `react-reader`)
- 🧭 Окно навигации по главам (TOC)
- 🔤 Окно настроек:
  - изменение **размера шрифта**
  - изменение **межстрочного интервала**
  - режимы отображения (Показать текст в 2 колонки, Прокрутка текста)
- 💾 Сохранение прогресса чтения и настроек (через `localStorage`)
- ⏳ Индикатор загрузки книги (progress)

### PDF
- 📄 Чтение PDF (через `react-pdf` / `pdfjs`)
- 🔎 Zoom In / Zoom Out (масштабирование)
- ⬅️➡️ Перелистывание страниц:
  - кнопками в интерфейсе
  - стрелками клавиатуры `←` / `→`
- 🧭 Навигация по страницам через боковое меню (миниатюры страниц)
- 💾 Сохранение текущей страницы (через `localStorage`)
- ⏳ Индикатор загрузки книги (progress)

## Требования
- Node.js 18+ (рекомендуется)
- npm / yarn / pnpm

## Установка

    npm install

## Запуск в режиме разработки

    npm run dev

## Важно!

Сейчас для демо вначале рендерятся только две кнопки, при нажатии показывается epub или pdf читалка. Эти кнопки и <Reader/> под ними нужно удалить (есть TODO)

Для того чтобы демо работало, пришлось некоторые переменные заменить моками:

- в `App.tsx` есть `// TODO: заменить на реальные данные`
- в том же файле ниже есть несколько `TODO:`, которые надо раскомментировать
- папку `bookExamples` можно будет потом удалить
